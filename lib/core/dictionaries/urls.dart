const String srv = 'jsonplaceholder.typicode.com';

Uri users = Uri.https(srv, '/users');
Uri posts = Uri.https(srv, '/posts');
Uri albums = Uri.https(srv, '/albums');
Uri comments = Uri.https(srv, '/comments');